type CatalogType = {
  id: String;
  appUUID: String;
  microsite: Boolean;
  whatsapp: Boolean;
  products: Object;
  published: Boolean;
  createdAt: String;
  updateAt: String;
};

export default CatalogType;
